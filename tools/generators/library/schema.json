{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "library",
  "title": "Create Library - mstable",
  "description": "Create a React Library for mStable workspace",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the library",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the library?",
      "pattern": "^[a-zA-Z].*$"
    },
    "directory": {
      "type": "string",
      "description": "A directory where the lib is placed.",
      "alias": "dir"
    }
  },
  "required": ["name"]
}
