import { Stack, Typography } from '@mui/material';

export const Home = () => {
  return (
    <Stack
      direction="column"
      sx={{
        width: 1,
        height: 1,
      }}
    >
      <Typography>Home test</Typography>
    </Stack>
  );
};
